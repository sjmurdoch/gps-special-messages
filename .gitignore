# Julia
*.jl.cov
*.jl.*.cov
*.jl.mem
LocalPreferences.toml
JuliaLocalPreferences.toml
# Manifest.toml is COMMITTED in this repo for exact dependency pinning.

# OS
.DS_Store
.AppleDouble

# Data fetched from Zenodo or rebuilt by pipeline/
data/raw/
data/arrow/
data/processed/
data/ops_advisories
data/messages.duckdb
data/messages.duckdb.wal
data/messages.duckdb.tmp/
data/*.duckdb.old*

# Build artefact only — rebuilt from PNG/PDF sources
figures/output/*.svg

# Editor / scratch
*.swp
.idea/
.vscode/
